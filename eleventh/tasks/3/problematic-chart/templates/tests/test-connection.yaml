apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "problematic-chart.fullname" . }}-test-connection"
  labels:
    {{- include "problematic-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "problematic-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
