apiVersion: v1
kind: Service
metadata:
  name: {{ include "problematic-chart.fullname" . }}
  labels:
    {{- include "problematic-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "problematic-chart.selectorLabels" . | nindent 4 }}
