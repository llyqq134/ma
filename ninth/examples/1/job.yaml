apiVersion: batch/v1 
kind: Job 
metadata:
  name: simple-job 
spec:
  template:
    spec:
      containers:
      - name: task 
        image: busybox 
        command: ["echo", "job is done"]
      restartPolicy: Never
