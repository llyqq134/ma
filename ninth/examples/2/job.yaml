apiVersion: batch/v1 
kind: Job 
metadata:
  name: retry-job 
spec:
  backoffLimit: 3
  template:
    spec:
      containers:
      - name: processor 
        image: busybox 
        command: ["sh", "-c", "echo Try to exec...; exit 1"]
      restartPolicy: OnFailure
