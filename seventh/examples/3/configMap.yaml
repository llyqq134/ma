apiVersion: v1
kind: ConfigMap
metadata:
  name: app-file-config
data:
  application.properties: |
    server.port=8080
    spring.datasource.url=jdbc:postgresql://localhost/test
  config.ini: |
    [database]
    host=localhost
    port=5432
