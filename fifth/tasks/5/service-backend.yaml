apiVersion: v1
kind: Service
metadata:
  name: backend-service 
  labels: 
    environment: production
spec:
  selector:
    app: prod-backend 
  type: ClusterIP
  ports:
  - name: backend-service 
    protocol: TCP
    port: 8080
    targetPort: 8080 

