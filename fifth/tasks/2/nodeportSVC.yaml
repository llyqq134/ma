apiVersion: v1
kind: Service 
metadata:
  name: web-nodeport
  labels:
    service-type: external
spec:
  type: NodePort 
  selector:
    app: web-app
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30090
