apiVersion: v1
kind: Pod
metadata:
  name: explore-pod
spec:
  containers:
    - name: explore-pod
      image: busybox:latest
      ports:
        - containerPort: 80
      command: ["sh", "-c", "sleep 3600"]
